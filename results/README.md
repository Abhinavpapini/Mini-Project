# Results

This folder stores experiment outputs and summary files.

## Quick links
- SVM summary: `svm_op.txt`
- Wav2Vec as features summary: `wav2vec_as_feature_op.txt`
- Canonical experiment tracker: `experiment_tracker.csv`
- Week-by-week execution schedule: `experiment_execution_schedule.md`

## Notes
- Keep generated artifacts under `artifacts/`.
- Track only core plan items (plan_order 1-35) until completion sign-off.
- Post-35 queue items (plan_order 36-40) are deferred and should run only after core completion.
- Append every completed run to `experiment_tracker.csv` with run_id, fold, config, and metrics.
